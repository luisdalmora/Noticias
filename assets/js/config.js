const CONFIG = {
  USE_ONLINE_MODE: false, // Inicia em false por padrão até que um endpoint seja configurado
  ONLINE_NEWS_URL: "",    // Colocar URL do endpoint JSON aqui
  LOCAL_NEWS_URL: "data/noticias.json",
  LOCAL_SOURCES_URL: "data/fontes.json",
  FALLBACK_LOCAL_JSON: true,
  REQUEST_TIMEOUT: 12000, // 12 segundos
  APP_VERSION: "2.2.0 STATIC OS",
  DEBUG_MODE: true
};
